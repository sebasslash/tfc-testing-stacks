# pet-nulls

This is a stack using two components, two deployments, and the `random_pet` and `null_resource` resources. The purpose is a no-authentication, state-only test of the stacks concepts.

Fork this repository to test stacks, or as a starting point for your real configuration.
